import { Text } from '@chakra-ui/react'
import MenuLink from './menu-link'

const MenuList = ({  }) => (
    <>
        <MenuLink title= 'O mnie' route= '/about'/>
        <MenuLink title= 'Projekty' route= '/projects'/>
    </>
   
)

export default MenuList